pipeline {
    agent any

    stages {
        stage('1 npm install') {
            steps {
                echo 'npm install ..'
            }
        }
        stage('2 nugget install') {
            steps {
                echo 'nvm install..'
            }
        }
    }
}